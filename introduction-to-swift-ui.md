**struct**  - container for variables, functions and behaviors.

```swift
struct ContentView: View {
    var body: some View {
        Text("Hello, World :)")
    }
}

// View defines that this struct behaves like a view.
```

**View** - is a retangle on the screen.

**Prop** = var 
